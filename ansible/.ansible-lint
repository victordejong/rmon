---
# Ansible-lint does not fail on warnings from the rules or tags listed below
skip_list:
  - fqcn[action-core]
  - name[template]
  - role-name
  - var-naming[no-role-prefix]

exclude_paths:
  - .venv/
